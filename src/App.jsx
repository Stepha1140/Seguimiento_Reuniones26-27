import React, { useEffect, useMemo, useState } from "react";
import * as XLSX from "xlsx";
import { supabase } from "./supabase.js";

const ZONAS=["Metropolitana","Suroccidente","Suroriente","Riomar","Centro Histórico"];
const E={page:{minHeight:"100vh",background:"#f4f7fb",fontFamily:"Arial,sans-serif",color:"#172033"},header:{background:"white",padding:20,borderBottom:"1px solid #dbe2ea"},content:{maxWidth:1180,margin:"0 auto",padding:24},row:{display:"flex",gap:12,flexWrap:"wrap",alignItems:"center"},grid:{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(290px,1fr))",gap:18},card:{background:"white",border:"1px solid #dbe2ea",borderRadius:16,padding:20,boxShadow:"0 6px 18px rgba(15,23,42,.06)"},input:{width:"100%",boxSizing:"border-box",padding:11,marginTop:6,border:"1px solid #b9c4d2",borderRadius:10},btn:{border:0,borderRadius:10,padding:"11px 15px",cursor:"pointer",fontWeight:700},blue:{background:"#2563eb",color:"white"},white:{background:"white",border:"1px solid #b9c4d2",color:"#172033"},red:{background:"#fff1f2",border:"1px solid #fecdd3",color:"#be123c"},green:{background:"#ecfdf5",color:"#047857"},orange:{background:"#fff7ed",color:"#9a3412"},modalBg:{position:"fixed",inset:0,zIndex:20,background:"rgba(15,23,42,.55)",display:"flex",alignItems:"center",justifyContent:"center",padding:16},modal:{width:"100%",maxWidth:1000,maxHeight:"92vh",overflowY:"auto",background:"white",borderRadius:18,padding:24},block:{background:"#f8fafc",border:"1px solid #e2e8f0",borderRadius:12,padding:14},table:{width:"100%",borderCollapse:"collapse",minWidth:760},cell:{padding:11,borderBottom:"1px solid #e2e8f0",textAlign:"left"}};
const LV={nombre:"",zona:"",telefono:""};
const RV={tema:"",descripcion:"",fecha:"",lugar:"",personas_convocadas:"",personas_asistentes:"",entrego_premios:false,observaciones:""};
const norm=v=>String(v??"").trim();
const safe=v=>norm(v).normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-zA-Z0-9._-]/g,"_");
const fecha=f=>f?new Date(f+"T00:00:00").toLocaleDateString("es-CO",{day:"2-digit",month:"long",year:"numeric"}):"Sin fecha";

export default function App(){
 const [session,setSession]=useState(null),[email,setEmail]=useState(""),[password,setPassword]=useState("");
 const [leaders,setLeaders]=useState([]),[meetings,setMeetings]=useState([]),[attendance,setAttendance]=useState([]),[people,setPeople]=useState([]);
 const [loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[msg,setMsg]=useState(""),[kind,setKind]=useState("green");
 const [search,setSearch]=useState(""),[zoneFilter,setZoneFilter]=useState("Todas"),[rankingZone,setRankingZone]=useState("Metropolitana"),[year,setYear]=useState(new Date().getFullYear()),[modal,setModal]=useState(""),[leader,setLeader]=useState(null),[evidenceMeeting,setEvidenceMeeting]=useState(null),[evidence,setEvidence]=useState([]);
 const [strategy,setStrategy]=useState("reuniones");
 const [cafeRecords,setCafeRecords]=useState([]);
 const [courseRecords,setCourseRecords]=useState([]);
 const [courseAttendance,setCourseAttendance]=useState([]),[strategyEvidence,setStrategyEvidence]=useState([]);
 const [lf,setLf]=useState(LV),[rf,setRf]=useState(RV),[excel,setExcel]=useState(null),[winners,setWinners]=useState(null),[photos,setPhotos]=useState([]),[excelRows,setExcelRows]=useState([]);
 const say=(t,k="green")=>{setMsg(t);setKind(k)};
 useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);setLoading(false)});const {data}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>data.subscription.unsubscribe()},[]);
 useEffect(()=>{if(session)load();else{setLeaders([]);setMeetings([]);setAttendance([]);setPeople([])}},[session]);
 async function load(){
  setLoading(true);
  const [a,b,c,d,e,f,g,h]=await Promise.all([
   supabase.from("lideres").select("*").order("nombre"),
   supabase.from("reuniones").select("*").order("fecha",{ascending:false}),
   supabase.from("asistencias").select("*"),
   supabase.from("personas").select("*"),
   supabase.from("cafes_barrio").select("*").order("fecha",{ascending:false}),
   supabase.from("cursos").select("*").order("fecha",{ascending:false}),
   supabase.from("asistentes_cursos").select("*"),
   supabase.from("evidencias_estrategias").select("*")
  ]);
  for(const x of [a,b,c,d,e,f,g,h])if(x.error)say(x.error.message,"red");
  const leaderMap=new Map((a.data||[]).map(l=>[String(l.id),l]));
  setLeaders(a.data||[]);setMeetings(b.data||[]);setAttendance(c.data||[]);setPeople(d.data||[]);
  setCafeRecords((e.data||[]).map(r=>({...r,lider_nombre:leaderMap.get(String(r.lider_id))?.nombre||"",convocados:r.personas_convocadas,asistentes:r.personas_asistentes,fotos:(h.data||[]).filter(x=>x.cafe_id===r.id)})));
  setCourseRecords((f.data||[]).map(r=>({...r,lider_nombre:leaderMap.get(String(r.lider_id))?.nombre||"",participantes:r.participantes_inscritos,asistentes:(g.data||[]).filter(x=>x.curso_id===r.id).length,lista:(g.data||[]).filter(x=>x.curso_id===r.id).map(x=>{const person=(d.data||[]).find(p=>p.id===x.persona_id)||{};return{documento:person.numero_documento,nombre:person.nombre_completo,telefono:person.telefono}}),evidencias:(h.data||[]).filter(x=>x.curso_id===r.id&&x.tipo==="EVIDENCIA"),certificados:(h.data||[]).filter(x=>x.curso_id===r.id&&x.tipo==="CERTIFICADO")})));
  setCourseAttendance(g.data||[]);setStrategyEvidence(h.data||[]);setLoading(false)
 }
 async function login(e){e.preventDefault();setSaving(true);const {error}=await supabase.auth.signInWithPassword({email,password});if(error)say(error.message,"red");setSaving(false)}
 async function saveLeader(e){e.preventDefault();setSaving(true);const {error}=await supabase.from("lideres").insert({...lf,estado:"Activo"});if(error)say(error.message,"red");else{setLf(LV);setModal("");say("Líder guardado");await load()}setSaving(false)}
 function openMeeting(l){setLeader(l);setRf(RV);setExcel(null);setWinners(null);setPhotos([]);setExcelRows([]);setMsg("");setModal("meeting")}
 async function readExcel(file){setExcel(file);if(!file){setExcelRows([]);return}try{const bytes=await file.arrayBuffer();const wb=XLSX.read(bytes,{type:"array"});const rows=XLSX.utils.sheet_to_json(wb.Sheets[wb.SheetNames[0]],{defval:""});const parsed=rows.map(r=>({tipo_documento:norm(r.TipoDocumento||r.tipo_documento),numero_documento:norm(r.NumeroDocumento||r.numero_documento||r.Documento),nombre_completo:norm(r.NombreCompleto||r.nombre_completo||r.Nombre),telefono:norm(r.Telefono||r.telefono),zona:norm(r.Zona||r.zona)})).filter(r=>r.numero_documento&&r.nombre_completo);setExcelRows(parsed);say(`Excel leído: ${parsed.length} asistentes válidos.`)}catch(err){setExcelRows([]);say("No se pudo leer el Excel: "+err.message,"red")}}
 async function upload(file,id,type){if(!file)return;const path=`${session.user.id}/${id}/${type}_${Date.now()}_${safe(file.name)}`;const up=await supabase.storage.from("evidencias-reuniones").upload(path,file);if(up.error)throw up.error;const rec=await supabase.from("evidencias").insert({reunion_id:id,tipo:type,nombre_archivo:file.name,ruta_archivo:path});if(rec.error)throw rec.error}
 async function importAttendance(rows,meetingId){const unique=[...new Map(rows.map(r=>[r.numero_documento,r])).values()];for(const p of unique){const {data,error}=await supabase.from("personas").upsert(p,{onConflict:"numero_documento"}).select("id").single();if(error)throw error;const a=await supabase.from("asistencias").upsert({persona_id:data.id,reunion_id:meetingId},{onConflict:"persona_id,reunion_id"});if(a.error)throw a.error}}
 async function saveMeeting(e){e.preventDefault();if(+rf.personas_asistentes>+rf.personas_convocadas)return say("Asistentes no puede superar convocados","red");if(excel&&excelRows.length===0)return say("El Excel no tiene columnas válidas","red");if(excelRows.length&&excelRows.length!==+rf.personas_asistentes)return say(`El Excel contiene ${excelRows.length} personas únicas, pero registraste ${rf.personas_asistentes} asistentes. Corrige antes de guardar.`,"red");setSaving(true);const {data,error}=await supabase.from("reuniones").insert({...rf,lider_id:leader.id,personas_convocadas:+rf.personas_convocadas,personas_asistentes:+rf.personas_asistentes}).select().single();if(error){say(error.message,"red");setSaving(false);return}try{if(excelRows.length)await importAttendance(excelRows,data.id);await upload(excel,data.id,"LISTA_ASISTENTES");await upload(winners,data.id,"LISTA_PREMIADOS");for(const p of photos)await upload(p,data.id,"FOTOGRAFIA");say("Reunión, asistentes y evidencias guardados");setModal("");await load()}catch(err){say("Reunión creada, pero faltó procesar una evidencia: "+err.message,"orange");await load()}setSaving(false)}
 async function uploadStrategyFiles(files,strategy,recordId,type){
  for(const file of files||[]){
   const folder=strategy==="CAFE_BARRIO"?"cafes":"cursos";
   const path=`${session.user.id}/${folder}/${recordId}/${type}_${Date.now()}_${safe(file.name)}`;
   const up=await supabase.storage.from("evidencias-estrategias").upload(path,file);
   if(up.error)throw up.error;
   const row={estrategia:strategy,cafe_id:strategy==="CAFE_BARRIO"?recordId:null,curso_id:strategy==="CURSO"?recordId:null,tipo,nombre_archivo:file.name,ruta_archivo:path};
   const rec=await supabase.from("evidencias_estrategias").insert(row);if(rec.error)throw rec.error;
  }
 }
 async function remove(table,id,text){if(!confirm(text))return;const {error}=await supabase.from(table).delete().eq("id",id);if(error)say(error.message,"red");else await load()}
 async function viewEvidence(m){const {data,error}=await supabase.from("evidencias").select("*").eq("reunion_id",m.id);if(error)return say(error.message,"red");const out=await Promise.all((data||[]).map(async x=>{const r=await supabase.storage.from("evidencias-reuniones").createSignedUrl(x.ruta_archivo,3600);return{...x,url:r.data?.signedUrl||""}}));setEvidenceMeeting(m);setEvidence(out);setModal("evidence")}
 const leaderMeetings=id=>meetings.filter(m=>m.lider_id===id&&+m.fecha.slice(0,4)===+year);
 const filtered=useMemo(()=>leaders.filter(l=>{
  const matchesSearch=`${l.nombre} ${l.zona} ${l.telefono}`.toLowerCase().includes(search.toLowerCase());
  const matchesZone=zoneFilter==="Todas"||norm(l.zona).toLowerCase()===zoneFilter.toLowerCase();
  return matchesSearch&&matchesZone
 }),[leaders,search,zoneFilter]);
 const zones=useMemo(()=>{const rows=ZONAS.map(z=>{const ids=new Set(leaders.filter(l=>norm(l.zona).toLowerCase()===z.toLowerCase()).map(l=>l.id));const ms=meetings.filter(m=>ids.has(m.lider_id)&&+m.fecha.slice(0,4)===+year);return{zona:z,lideres:ids.size,reuniones:ms.length,convocados:ms.reduce((s,m)=>s+m.personas_convocadas,0),asistentes:ms.reduce((s,m)=>s+m.personas_asistentes,0)}});const total=rows.reduce((s,r)=>s+r.asistentes,0);return rows.map(r=>({...r,asistencia:r.convocados?Math.round(r.asistentes/r.convocados*100):0,peso:total?Math.round(r.asistentes/total*100):0})).sort((a,b)=>b.asistentes-a.asistentes)},[leaders,meetings,year]);
 function leaderStats(id){const ms=leaderMeetings(id),ids=new Set(ms.map(m=>m.id)),as=attendance.filter(a=>ids.has(a.reunion_id)),counts={};as.forEach(a=>counts[a.persona_id]=(counts[a.persona_id]||0)+1);return{meetings:ms,conv:ms.reduce((s,m)=>s+m.personas_convocadas,0),att:ms.reduce((s,m)=>s+m.personas_asistentes,0),unique:Object.keys(counts).length,repeated:Object.values(counts).filter(n=>n>1).length,counts}}
 function frequencyRows(id){const st=leaderStats(id);return Object.entries(st.counts).map(([pid,n])=>{const p=people.find(x=>x.id===+pid)||{};return{...p,veces:n}}).sort((a,b)=>b.veces-a.veces)}
 function zoneLeaderRows(zone){return leaders.filter(l=>norm(l.zona).toLowerCase()===zone.toLowerCase()).map(l=>{const s=leaderStats(l.id);return{nombre:l.nombre,reuniones:s.meetings.length,convocados:s.conv,asistentes:s.att,noAsistieron:s.conv-s.att,asistencia:s.conv?Math.round(s.att/s.conv*100):0,unicas:s.unique,repetidas:s.repeated}}).sort((a,b)=>b.asistentes-a.asistentes||b.asistencia-a.asistencia)}
 function csvValue(value){const text=String(value??"").replace(/"/g,'""');return `"${text}"`}
 function downloadCsv(filename,headers,rows){const content="\ufeff"+[headers.map(csvValue).join(";")].concat(rows.map(row=>row.map(csvValue).join(";"))).join("\r\n");const url=URL.createObjectURL(new Blob([content],{type:"text/csv;charset=utf-8"}));const link=document.createElement("a");link.href=url;link.download=filename;link.click();URL.revokeObjectURL(url)}
 function exportAll(){downloadCsv(`reuniones_${year}.csv`,["Líder","Zona","Teléfono","Reunión","Fecha","Lugar","Convocados","Asistentes","No asistieron","Asistencia %","Premios","Descripción","Observaciones"],meetings.filter(m=>+m.fecha.slice(0,4)===+year).map(m=>{const l=leaders.find(x=>x.id===m.lider_id)||{};return[l.nombre,l.zona,l.telefono,m.tema,m.fecha,m.lugar,m.personas_convocadas,m.personas_asistentes,m.personas_convocadas-m.personas_asistentes,m.personas_convocadas?Math.round(m.personas_asistentes/m.personas_convocadas*100):0,m.entrego_premios?"Sí":"No",m.descripcion,m.observaciones]}))}
 const notice=kind==="red"?E.red:kind==="orange"?E.orange:E.green;
 if(loading&&!session)return <main style={E.page}><div style={E.content}>Cargando...</div></main>;
 if(!session)return <main style={{...E.page,display:"flex",alignItems:"center",justifyContent:"center",padding:20}}><form style={{...E.card,width:"100%",maxWidth:430}} onSubmit={login}><h1>Seguimiento de Estrategias</h1>{msg&&<p style={{...E.block,...notice}}>{msg}</p>}<label>Correo<input required type="email" style={E.input} value={email} onChange={e=>setEmail(e.target.value)}/></label><br/><br/><label>Contraseña<input required type="password" style={E.input} value={password} onChange={e=>setPassword(e.target.value)}/></label><br/><br/><button style={{...E.btn,...E.blue,width:"100%"}}>{saving?"Ingresando...":"Iniciar sesión"}</button></form></main>;
 return <div style={{...E.page,display:"flex",alignItems:"stretch"}}>
  <aside style={{width:260,minWidth:260,minHeight:"100vh",background:"#0f172a",color:"white",padding:20,boxSizing:"border-box",position:"sticky",top:0,alignSelf:"flex-start"}}>
   <h2 style={{margin:"0 0 6px"}}>Estrategias</h2><p style={{margin:"0 0 22px",fontSize:13,color:"#cbd5e1"}}>Panel de seguimiento</p>
   {[["dashboard","Dashboard"],["reuniones","Reuniones"],["cafe","Café a tu Barrio"],["cursos","Cursos"]].map(([id,label])=><button key={id} onClick={()=>setStrategy(id)} style={{...E.btn,width:"100%",marginBottom:10,textAlign:"left",background:strategy===id?"#2563eb":"#1e293b",color:"white"}}>{label}</button>)}
   <button onClick={()=>supabase.auth.signOut()} style={{...E.btn,width:"100%",marginTop:18,textAlign:"left",background:"transparent",border:"1px solid #475569",color:"white"}}>Cerrar sesión</button>
  </aside><div style={{flex:1,minWidth:0}}>
  {strategy==="dashboard"&&<DashboardModule meetings={meetings} attendance={attendance} people={people} cafeRecords={cafeRecords} courseRecords={courseRecords} year={year} setYear={setYear} downloadCsv={downloadCsv}/>}
  {strategy==="reuniones"&&<>
  <header style={E.header}><div style={{maxWidth:1180,margin:"0 auto",...E.row,justifyContent:"space-between"}}><div><h1 style={{margin:0}}>Seguimiento de Estrategias</h1><p style={{marginBottom:0,color:"#64748b"}}>Reuniones, asistencia, frecuencia y rendimiento</p></div><div style={E.row}><button style={{...E.btn,...E.blue}} onClick={()=>setModal("leader")}>+ Agregar líder</button><button style={{...E.btn,...E.white}} onClick={()=>setModal("zones")}>Rendimiento por zonas</button><button style={{...E.btn,...E.white}} onClick={()=>setModal("zoneLeaders")}>Líderes por zona</button><button style={{...E.btn,...E.white}} onClick={exportAll}>Descargar información</button></div></div></header>
  <main style={E.content}>{msg&&<p style={{...E.block,...notice}}>{msg}</p>}<div style={{...E.card,...E.row,marginBottom:20}}><input style={{...E.input,flex:1,marginTop:0,minWidth:220}} placeholder="Buscar líder" value={search} onChange={e=>setSearch(e.target.value)}/><select style={{...E.input,width:210,marginTop:0}} value={zoneFilter} onChange={e=>setZoneFilter(e.target.value)}><option value="Todas">Todas las zonas</option>{ZONAS.map(z=><option key={z} value={z}>{z}</option>)}</select><select style={{...E.input,width:130,marginTop:0}} value={year} onChange={e=>setYear(+e.target.value)}>{[2025,2026,2027,2028].map(y=><option key={y}>{y}</option>)}</select></div>{filtered.length===0&&<p style={E.block}>No hay líderes registrados en la zona seleccionada.</p>}<div style={E.grid}>{filtered.map(l=>{const st=leaderStats(l.id);return <section key={l.id} style={E.card}><h2>{l.nombre}</h2><p><b>Zona:</b> {l.zona}</p><p><b>Teléfono:</b> {l.telefono}</p><p><b>Reuniones:</b> {st.meetings.length}</p><div style={E.row}><button style={{...E.btn,...E.blue}} onClick={()=>openMeeting(l)}>Registrar reunión</button><button style={{...E.btn,...E.white}} onClick={()=>{setLeader(l);setModal("history")}}>Ver reuniones</button><button style={{...E.btn,...E.white}} onClick={()=>{setLeader(l);setModal("performance")}}>Rendimiento</button><button style={{...E.btn,...E.white}} onClick={()=>{setLeader(l);setModal("frequency")}}>Repetidos</button><button style={{...E.btn,...E.red}} onClick={()=>remove("lideres",l.id,"¿Eliminar líder?")}>Eliminar</button></div></section>})}</div></main>
  </>}
  {strategy==="cafe"&&<CafeModule leaders={leaders} records={cafeRecords} reload={load} uploadFiles={uploadStrategyFiles}/>}
  {strategy==="cursos"&&<CoursesModule leaders={leaders} records={courseRecords} reload={load} uploadFiles={uploadStrategyFiles}/>}
  {modal==="leader"&&<div style={E.modalBg}><form style={E.modal} onSubmit={saveLeader}><h2>Agregar líder</h2><label>Nombre *<input required style={E.input} value={lf.nombre} onChange={e=>setLf({...lf,nombre:e.target.value})}/></label><br/><br/><label>Zona *<select required style={E.input} value={lf.zona} onChange={e=>setLf({...lf,zona:e.target.value})}><option value="">Seleccione</option>{ZONAS.map(z=><option key={z}>{z}</option>)}</select></label><br/><br/><label>Teléfono<input style={E.input} value={lf.telefono} onChange={e=>setLf({...lf,telefono:e.target.value})}/></label><br/><br/><div style={E.row}><button type="button" style={{...E.btn,...E.white}} onClick={()=>setModal("")}>Cancelar</button><button style={{...E.btn,...E.blue}}>Guardar</button></div></form></div>}
  {modal==="meeting"&&leader&&<div style={E.modalBg}><form style={E.modal} onSubmit={saveMeeting}><h2>Registrar reunión y evidencias</h2><p><b>Líder:</b> {leader.nombre}</p>{msg&&<p style={{...E.block,...notice}}>{msg}</p>}<label>Tema *<input required style={E.input} value={rf.tema} onChange={e=>setRf({...rf,tema:e.target.value})}/></label><br/><br/><label>Descripción *<textarea required rows="3" style={E.input} value={rf.descripcion} onChange={e=>setRf({...rf,descripcion:e.target.value})}/></label><br/><br/><div style={E.row}><label style={{flex:1}}>Fecha *<input required type="date" style={E.input} value={rf.fecha} onChange={e=>setRf({...rf,fecha:e.target.value})}/></label><label style={{flex:1}}>Lugar<input style={E.input} value={rf.lugar} onChange={e=>setRf({...rf,lugar:e.target.value})}/></label></div><br/><div style={E.row}><label style={{flex:1}}>Convocados *<input required type="number" min="0" style={E.input} value={rf.personas_convocadas} onChange={e=>setRf({...rf,personas_convocadas:e.target.value})}/></label><label style={{flex:1}}>Asistentes *<input required type="number" min="0" style={E.input} value={rf.personas_asistentes} onChange={e=>setRf({...rf,personas_asistentes:e.target.value})}/></label></div><br/><div style={E.block}><b>Excel de asistentes</b><p>Columnas: TipoDocumento, NumeroDocumento, NombreCompleto, Telefono, Zona.</p><input type="file" accept=".xlsx,.xls,.csv" onChange={e=>readExcel(e.target.files?.[0]||null)}/>{excel&&<p>{excel.name}: <b>{excelRows.length} personas válidas</b></p>}</div><br/><div style={E.block}><b>Fotografías</b><br/><input type="file" accept="image/*" multiple onChange={e=>setPhotos([...e.target.files])}/></div><br/><div style={E.block}><label><input type="checkbox" checked={rf.entrego_premios} onChange={e=>setRf({...rf,entrego_premios:e.target.checked})}/> Se entregaron premios</label>{rf.entrego_premios&&<div><p>Excel de premiados</p><input type="file" accept=".xlsx,.xls,.csv" onChange={e=>setWinners(e.target.files?.[0]||null)}/></div>}</div><br/><label>Observaciones<textarea rows="3" style={E.input} value={rf.observaciones} onChange={e=>setRf({...rf,observaciones:e.target.value})}/></label><br/><br/><div style={E.row}><button type="button" style={{...E.btn,...E.white}} onClick={()=>setModal("")}>Cancelar</button><button style={{...E.btn,...E.blue}} disabled={saving}>{saving?"Guardando...":"Guardar"}</button></div></form></div>}
  {modal==="zones"&&<div style={E.modalBg}><div style={E.modal}><div style={{...E.row,justifyContent:"space-between"}}><h2>Rendimiento por zonas</h2><div style={E.row}><button style={{...E.btn,...E.blue}} onClick={()=>downloadCsv(`rendimiento_zonas_${year}.csv`,["Zona","Líderes","Reuniones","Convocados","Asistentes","Asistencia %","Peso %"],zones.map(z=>[z.zona,z.lideres,z.reuniones,z.convocados,z.asistentes,z.asistencia,z.peso]))}>Descargar</button><button style={{...E.btn,...E.white}} onClick={()=>setModal("")}>Cerrar</button></div></div><Table headers={["Zona","Líderes","Reuniones","Convocados","Asistentes","Asistencia","Peso"]} rows={zones.map(z=>[z.zona,z.lideres,z.reuniones,z.convocados,z.asistentes,z.asistencia+"%",z.peso+"%"])}/></div></div>}
  {modal==="zoneLeaders"&&<div style={E.modalBg}><div style={E.modal}><div style={{...E.row,justifyContent:"space-between"}}><div><h2 style={{marginBottom:4}}>Rendimiento de líderes por zona</h2><p style={{marginTop:0,color:"#64748b"}}>Comparación correspondiente a {year}</p></div><div style={E.row}><button style={{...E.btn,...E.blue}} onClick={()=>downloadCsv(`lideres_${rankingZone}_${year}.csv`,["Posición","Líder","Reuniones","Convocados","Asistentes","No asistieron","Asistencia %","Personas únicas","Repetidas"],zoneLeaderRows(rankingZone).map((r,i)=>[i+1,r.nombre,r.reuniones,r.convocados,r.asistentes,r.noAsistieron,r.asistencia,r.unicas,r.repetidas]))}>Descargar</button><button style={{...E.btn,...E.white}} onClick={()=>setModal("")}>Cerrar</button></div></div><div style={{...E.block,...E.row,marginBottom:18}}><label style={{minWidth:280}}><b>Seleccione una zona</b><select style={E.input} value={rankingZone} onChange={e=>setRankingZone(e.target.value)}>{ZONAS.map(z=><option key={z} value={z}>{z}</option>)}</select></label></div>{zoneLeaderRows(rankingZone).length>0?<><div style={{...E.block,...E.green,marginBottom:16}}><b>Líder con mayor asistencia:</b> {zoneLeaderRows(rankingZone)[0].nombre}, con {zoneLeaderRows(rankingZone)[0].asistentes} asistentes registrados.</div><Table headers={["Posición","Líder","Reuniones","Convocados","Asistentes","No asistieron","Asistencia","Personas únicas","Repetidas"]} rows={zoneLeaderRows(rankingZone).map((r,i)=>[i+1,r.nombre,r.reuniones,r.convocados,r.asistentes,r.noAsistieron,r.asistencia+"%",r.unicas,r.repetidas])}/></>:<p style={E.block}>No hay líderes registrados en {rankingZone}.</p>}<p style={{fontSize:13,color:"#64748b",marginTop:16}}>La clasificación se ordena por asistentes acumulados y, en caso de empate, por porcentaje de asistencia. Personas únicas y repetidas dependen de los Excel importados.</p></div></div>}
  {modal==="performance"&&leader&&(()=>{const s=leaderStats(leader.id);return <div style={E.modalBg}><div style={E.modal}><div style={{...E.row,justifyContent:"space-between"}}><h2>Rendimiento de {leader.nombre}</h2><button style={{...E.btn,...E.white}} onClick={()=>setModal("")}>Cerrar</button></div><div style={E.grid}>{[["Reuniones",s.meetings.length],["Convocados",s.conv],["Asistentes",s.att],["No asistieron",s.conv-s.att],["Asistencia",(s.conv?Math.round(s.att/s.conv*100):0)+"%"],["Personas únicas",s.unique],["Personas repetidas",s.repeated]].map(([a,b])=><div key={a} style={E.block}><small>{a}</small><h2>{b}</h2></div>)}</div><h3>Rendimiento por reunión</h3><Table headers={["Reunión","Fecha","Convocados","Asistentes","No asistieron","Asistencia"]} rows={s.meetings.map(m=>[m.tema,fecha(m.fecha),m.personas_convocadas,m.personas_asistentes,m.personas_convocadas-m.personas_asistentes,(m.personas_convocadas?Math.round(m.personas_asistentes/m.personas_convocadas*100):0)+"%"])}/></div></div>})()}
  {modal==="frequency"&&leader&&<div style={E.modalBg}><div style={E.modal}><div style={{...E.row,justifyContent:"space-between"}}><h2>Frecuencia de asistentes: {leader.nombre}</h2><div style={E.row}><button style={{...E.btn,...E.blue}} onClick={()=>downloadCsv(`frecuencia_${safe(leader.nombre)}_${year}.csv`,["Documento","Nombre","Teléfono","Veces que asistió"],frequencyRows(leader.id).map(p=>[p.numero_documento,p.nombre_completo,p.telefono,p.veces]))}>Descargar</button><button style={{...E.btn,...E.white}} onClick={()=>setModal("")}>Cerrar</button></div></div><p>Solo aparecen personas importadas desde Excel con número de documento.</p><Table headers={["Documento","Nombre","Teléfono","Veces que asistió"]} rows={frequencyRows(leader.id).map(p=>[p.numero_documento,p.nombre_completo,p.telefono,p.veces])}/></div></div>}
  {modal==="history"&&leader&&<div style={E.modalBg}><div style={E.modal}><div style={{...E.row,justifyContent:"space-between"}}><h2>Reuniones de {leader.nombre}</h2><button style={{...E.btn,...E.white}} onClick={()=>setModal("")}>Cerrar</button></div>{leaderMeetings(leader.id).map(m=><article key={m.id} style={{...E.card,marginBottom:14}}><h3>{m.tema}</h3><p>{fecha(m.fecha)} | {m.lugar}</p><p>{m.descripcion}</p><p><b>Convocados:</b> {m.personas_convocadas} | <b>Asistentes:</b> {m.personas_asistentes} | <b>Asistencia:</b> {m.personas_convocadas?Math.round(m.personas_asistentes/m.personas_convocadas*100):0}%</p><div style={E.row}><button style={{...E.btn,...E.white}} onClick={()=>viewEvidence(m)}>Evidencias</button><button style={{...E.btn,...E.red}} onClick={()=>remove("reuniones",m.id,"¿Eliminar reunión?")}>Eliminar</button></div></article>)}</div></div>}
  {modal==="evidence"&&<div style={E.modalBg}><div style={E.modal}><div style={{...E.row,justifyContent:"space-between"}}><h2>Evidencias: {evidenceMeeting?.tema}</h2><button style={{...E.btn,...E.white}} onClick={()=>setModal("history")}>Regresar</button></div>{evidence.map(x=><div key={x.id} style={{...E.block,marginBottom:10}}><b>{x.tipo}</b><p>{x.nombre_archivo}</p>{x.url&&<a href={x.url} target="_blank" rel="noreferrer">Abrir o descargar</a>}</div>)}</div></div>}
  </div>
 </div>
}
function pct(value,total){return total?Math.round(value/total*100):0}
function PieChart({title,data,label}){const total=data.reduce((s,x)=>s+x.value,0);let used=0;const colors=data.map(x=>{const start=pct(used,total);used+=x.value;return `${x.color} ${start}% ${pct(used,total)}%`});return <section style={E.card}><h3 style={{marginTop:0,textAlign:"center"}}>{title}</h3><div style={{width:210,height:210,borderRadius:"50%",margin:"16px auto",background:total?`conic-gradient(${colors.join(",")})`:"#e2e8f0",display:"grid",placeItems:"center"}}><div style={{width:112,height:112,borderRadius:"50%",background:"white",display:"grid",placeItems:"center",textAlign:"center"}}><div><strong style={{fontSize:26}}>{total}</strong><br/><small>{label}</small></div></div></div>{data.map(x=><div key={x.name} style={{display:"flex",justifyContent:"space-between",gap:10,margin:"9px 0"}}><span><i style={{display:"inline-block",width:12,height:12,borderRadius:3,background:x.color,marginRight:7}}></i>{x.name}</span><b>{x.value} ({pct(x.value,total)}%)</b></div>)}</section>}
function MetricDetailCard({title,total,rows,field}){
 return <section style={E.card}>
  <small style={{color:"#64748b",fontSize:14}}>{title}</small>
  <h2 style={{fontSize:34,margin:"10px 0 16px",color:"#0f172a"}}>{total.toLocaleString("es-CO")}</h2>
  <div style={{borderTop:"1px solid #e2e8f0",paddingTop:10}}>
   {rows.map(row=><div key={row.key} style={{display:"flex",justifyContent:"space-between",alignItems:"center",gap:10,padding:"7px 0"}}>
    <span style={{display:"flex",alignItems:"center",color:"#475569",fontSize:14}}><i style={{width:11,height:11,borderRadius:3,background:row.color,marginRight:8,display:"inline-block"}}></i>{row.name}</span>
    <b>{row[field].toLocaleString("es-CO")}</b>
   </div>)}
  </div>
 </section>
}
function DateBarChart({title,data,field,color,suffix=""}){
 const max=Math.max(...data.map(x=>x[field]),1);
 return <section style={{...E.card,overflow:"hidden"}}><h3 style={{marginTop:0}}>{title}</h3><p style={{color:"#64748b",fontSize:13}}>Resultados ordenados cronológicamente por fecha.</p>{data.length?<div style={{overflowX:"auto",paddingBottom:8}}><div style={{display:"flex",alignItems:"flex-end",gap:14,minWidth:Math.max(520,data.length*86),height:280,borderBottom:"1px solid #cbd5e1",padding:"20px 8px 0"}}>{data.map((item,index)=>{const value=item[field],barHeight=value?Math.max(value/max*190,8):2;return <div key={item.date+index} title={`${item.label}: ${value}${suffix}`} style={{flex:"1 0 68px",maxWidth:90,height:"100%",display:"flex",flexDirection:"column",justifyContent:"flex-end",alignItems:"center"}}><b style={{fontSize:13,marginBottom:6,color:"#0f172a"}}>{value.toLocaleString("es-CO")}{suffix}</b><div style={{width:"70%",height:barHeight,background:color,borderRadius:"7px 7px 0 0",boxShadow:`0 4px 10px ${color}33`,transition:"height .3s ease"}}></div><small style={{marginTop:8,textAlign:"center",color:"#475569",whiteSpace:"nowrap"}}>{item.shortDate}</small></div>})}</div></div>:<p style={E.block}>No hay registros para el período seleccionado.</p>}</section>
}
function DashboardModule({meetings,attendance,people,cafeRecords,courseRecords,year,setYear,downloadCsv}){
 const META_PERSONAS=20000;
 const [dashboardFilter,setDashboardFilter]=useState("general");
 const rm=meetings.filter(x=>String(x.fecha||"").slice(0,4)===String(year));
 const rc=cafeRecords.filter(x=>String(x.fecha||"").slice(0,4)===String(year));
 const rk=courseRecords.filter(x=>String(x.fecha||"").slice(0,4)===String(year));
 const allRows=[
  {key:"reuniones",name:"Reuniones",color:"#2563eb",activities:rm.length,target:rm.reduce((s,x)=>s+(+x.personas_convocadas||0),0),people:rm.reduce((s,x)=>s+(+x.personas_asistentes||0),0)},
  {key:"cafe",name:"Café a tu Barrio",color:"#f59e0b",activities:rc.length,target:rc.reduce((s,x)=>s+(+x.convocados||0),0),people:rc.reduce((s,x)=>s+(+x.asistentes||0),0)},
  {key:"cursos",name:"Cursos",color:"#10b981",activities:rk.length,target:rk.reduce((s,x)=>s+(+x.participantes||0),0),people:rk.reduce((s,x)=>s+(+x.asistentes||0),0)}
 ];
 const visibleRows=dashboardFilter==="general"?allRows:allRows.filter(x=>x.key===dashboardFilter);
 const totalA=visibleRows.reduce((s,x)=>s+x.activities,0);
 const totalT=visibleRows.reduce((s,x)=>s+x.target,0);
 const totalP=visibleRows.reduce((s,x)=>s+x.people,0);
 const generalPeople=allRows.reduce((s,x)=>s+x.people,0);
 const faltantesMeta=Math.max(META_PERSONAS-generalPeople,0);
 const avanceMeta=Math.min(generalPeople/META_PERSONAS*100,100);
 const avanceTexto=avanceMeta.toLocaleString("es-CO",{minimumFractionDigits:2,maximumFractionDigits:2});

 const personById=new Map(people.map(p=>[String(p.id),p]));
 const meetingIds=new Set(rm.map(m=>String(m.id)));
 const meetingKeys=attendance.filter(a=>meetingIds.has(String(a.reunion_id))).map(a=>{
  const person=personById.get(String(a.persona_id));
  return norm(person?.numero_documento)||`persona-${a.persona_id}`;
 });
 const courseKeys=rk.flatMap(c=>(c.lista||[]).map(person=>norm(person.documento)).filter(Boolean));
 const identityKeys=dashboardFilter==="reuniones"?meetingKeys:dashboardFilter==="cursos"?courseKeys:dashboardFilter==="cafe"?[]:[...meetingKeys,...courseKeys];
 const identityCounts={};
 identityKeys.forEach(key=>identityCounts[key]=(identityCounts[key]||0)+1);
 const personasNuevas=Object.keys(identityCounts).length;
 const participacionesRepetidas=Math.max(identityKeys.length-personasNuevas,0);
 const personasQueRepitieron=Object.values(identityCounts).filter(n=>n>1).length;
 const hasNominalData=dashboardFilter!=="cafe";
 const best=[...visibleRows].sort((a,b)=>pct(b.people,b.target)-pct(a.people,a.target))[0];
 const selectedColor=visibleRows[0]?.color||"#2563eb";
 const strategyRecords=dashboardFilter==="reuniones"?rm.map(x=>({date:x.fecha,target:+x.personas_convocadas||0,people:+x.personas_asistentes||0})):dashboardFilter==="cafe"?rc.map(x=>({date:x.fecha,target:+x.convocados||0,people:+x.asistentes||0})):dashboardFilter==="cursos"?rk.map(x=>({date:x.fecha,target:+x.participantes||0,people:+x.asistentes||0})):[];
 const dateMap={};
 strategyRecords.forEach(item=>{const key=item.date||"Sin fecha";if(!dateMap[key])dateMap[key]={date:key,activities:0,target:0,people:0};dateMap[key].activities+=1;dateMap[key].target+=item.target;dateMap[key].people+=item.people});
 const dateRows=Object.values(dateMap).sort((a,b)=>String(a.date).localeCompare(String(b.date))).map(item=>({...item,performance:pct(item.people,item.target),label:item.date==="Sin fecha"?item.date:fecha(item.date),shortDate:item.date==="Sin fecha"?item.date:new Date(item.date+"T00:00:00").toLocaleDateString("es-CO",{day:"2-digit",month:"short"})}));

 function exportData(){
  const data=visibleRows.map(x=>[x.name,x.activities,x.target,x.people,pct(x.people,x.target),pct(x.people,totalP)]);
  data.push(["Personas identificadas únicas","","",personasNuevas,"",""]);
  data.push(["Participaciones repetidas","","",participacionesRepetidas,"",""]);
  data.push(["Meta general","","",META_PERSONAS,"Avance "+avanceTexto+"%","Faltan "+faltantesMeta]);
  downloadCsv(`dashboard_${dashboardFilter}_${year}.csv`,["Estrategia","Actividades","Convocados o inscritos","Asistentes o beneficiarios","Rendimiento %","Peso %"],data)
 }
 return <main style={E.content}>
  <div style={{...E.row,justifyContent:"space-between",marginBottom:20}}>
   <div><h1 style={{marginBottom:4}}>Dashboard interactivo de estrategias</h1><p style={{marginTop:0,color:"#64748b"}}>Consulta la información general o filtra una estrategia específica.</p></div>
   <div style={E.row}>
    <select style={{...E.input,width:220,marginTop:0}} value={dashboardFilter} onChange={e=>setDashboardFilter(e.target.value)}><option value="general">Vista general</option><option value="reuniones">Reuniones</option><option value="cafe">Café a tu Barrio</option><option value="cursos">Cursos</option></select>
    <select style={{...E.input,width:130,marginTop:0}} value={year} onChange={e=>setYear(+e.target.value)}>{[2025,2026,2027,2028].map(y=><option key={y} value={y}>{y}</option>)}</select>
    <button style={{...E.btn,...E.blue}} onClick={exportData}>Descargar vista</button>
   </div>
  </div>

  <section style={{...E.card,marginBottom:20,border:"1px solid #bfdbfe",background:"linear-gradient(135deg,#eff6ff,#fff)"}}>
   <div style={{display:"flex",justifyContent:"space-between",gap:18,flexWrap:"wrap"}}><div><small style={{color:"#1d4ed8",fontWeight:700}}>META GENERAL DE PERSONAS</small><h2 style={{fontSize:34,margin:"8px 0 4px"}}>{generalPeople.toLocaleString("es-CO")} de {META_PERSONAS.toLocaleString("es-CO")}</h2><span style={{color:"#64748b"}}>La meta usa el acumulado general de todas las estrategias en {year}.</span></div><div style={{textAlign:"right"}}><strong style={{display:"block",fontSize:32,color:"#2563eb"}}>{avanceTexto}%</strong><span style={{color:"#64748b"}}>Faltan {faltantesMeta.toLocaleString("es-CO")}</span></div></div>
   <div style={{height:18,background:"#dbeafe",borderRadius:999,overflow:"hidden",marginTop:20}}><div style={{height:"100%",width:`${avanceMeta}%`,background:"linear-gradient(90deg,#2563eb,#10b981)",borderRadius:999}}></div></div>
  </section>

  <div style={{...E.block,marginBottom:20,background:"#eef2ff",borderColor:"#c7d2fe"}}><b>Vista activa:</b> {dashboardFilter==="general"?"Todas las estrategias":visibleRows[0]?.name} · <b>Año:</b> {year}</div>

  <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(245px,1fr))",gap:18,marginBottom:20}}>
   <MetricDetailCard title="Actividades" total={totalA} rows={visibleRows} field="activities"/>
   <MetricDetailCard title="Convocados o inscritos" total={totalT} rows={visibleRows} field="target"/>
   <MetricDetailCard title="Asistentes o beneficiarios" total={totalP} rows={visibleRows} field="people"/>
   <section style={E.card}><small style={{color:"#64748b"}}>Rendimiento de la vista</small><h2 style={{fontSize:34,margin:"10px 0 16px"}}>{pct(totalP,totalT)}%</h2><div style={{borderTop:"1px solid #e2e8f0",paddingTop:10}}>{visibleRows.map(row=><div key={row.key} style={{display:"flex",justifyContent:"space-between",padding:"7px 0"}}><span style={{color:"#475569"}}>{row.name}</span><b>{pct(row.people,row.target)}%</b></div>)}</div></section>
  </div>

  <section style={{...E.card,marginBottom:20}}>
   <h2 style={{marginTop:0}}>Personas nuevas y repetidas</h2>
   {hasNominalData?<><p style={{color:"#64748b"}}>El cálculo usa los números de documento disponibles en los Excel de asistentes. “Participaciones repetidas” cuenta las asistencias adicionales de una persona después de su primera aparición durante el año seleccionado.</p><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(200px,1fr))",gap:14}}><div style={{...E.block,borderColor:"#a7f3d0",background:"#ecfdf5"}}><small>Personas identificadas únicas</small><h2 style={{marginBottom:0,color:"#047857"}}>{personasNuevas.toLocaleString("es-CO")}</h2></div><div style={{...E.block,borderColor:"#fed7aa",background:"#fff7ed"}}><small>Participaciones repetidas</small><h2 style={{marginBottom:0,color:"#c2410c"}}>{participacionesRepetidas.toLocaleString("es-CO")}</h2></div><div style={{...E.block,borderColor:"#ddd6fe",background:"#f5f3ff"}}><small>Personas que asistieron más de una vez</small><h2 style={{marginBottom:0,color:"#6d28d9"}}>{personasQueRepitieron.toLocaleString("es-CO")}</h2></div><div style={{...E.block}}><small>Registros nominales analizados</small><h2 style={{marginBottom:0}}>{identityKeys.length.toLocaleString("es-CO")}</h2></div></div></>:<div style={{...E.block,...E.orange}}>Café a tu Barrio guarda totales de convocados y asistentes, pero no una lista con documentos. Para discriminar personas nuevas y repetidas en esta estrategia se debe agregar un Excel nominal de participantes.</div>}
  </section>

  {best&&totalA>0&&<div style={{...E.block,...E.green,marginBottom:20}}><b>Mayor rendimiento en la vista:</b> {best.name}, con {pct(best.people,best.target)}%.</div>}
  {dashboardFilter==="general"?<div style={E.grid}>
   <PieChart title="Actividades" label="actividades" data={visibleRows.map(x=>({name:x.name,value:x.activities,color:x.color}))}/>
   <PieChart title="Convocados o inscritos" label="personas" data={visibleRows.map(x=>({name:x.name,value:x.target,color:x.color}))}/>
   <PieChart title="Asistentes o beneficiarios" label="personas" data={visibleRows.map(x=>({name:x.name,value:x.people,color:x.color}))}/>
   <PieChart title="Rendimiento" label="puntos %" data={visibleRows.map(x=>({name:x.name,value:pct(x.people,x.target),color:x.color}))}/>
  </div>:<div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(430px,1fr))",gap:18}}>
   <DateBarChart title="Actividades por fecha" data={dateRows} field="activities" color={selectedColor}/>
   <DateBarChart title="Convocados o inscritos por fecha" data={dateRows} field="target" color="#7c3aed"/>
   <DateBarChart title="Asistentes o beneficiarios por fecha" data={dateRows} field="people" color="#10b981"/>
   <DateBarChart title="Rendimiento por fecha" data={dateRows} field="performance" color="#f59e0b" suffix="%"/>
  </div>}
  <section style={{...E.card,marginTop:20}}><h2>Detalle comparativo</h2><Table headers={["Estrategia","Actividades","Convocados o inscritos","Asistentes o beneficiarios","Rendimiento","Peso"]} rows={visibleRows.map(x=>[x.name,x.activities,x.target,x.people,pct(x.people,x.target)+"%",pct(x.people,totalP)+"%"] )}/></section>
 </main>
}
function Field({label,children}){return <label style={{display:"block",flex:1,minWidth:220}}><b>{label}</b>{children}</label>}
function leaderData(leaders,id){return leaders.find(l=>String(l.id)===String(id))||{}}
function CafeModule({leaders,records,reload,uploadFiles}){
 const initial={lider_id:"",lider_nombre:"",zona:"",fecha:"",barrio:"",convocados:"",asistentes:"",descripcion:"",estado:"Realizada",fotos:[]}; const [form,setForm]=useState(initial),[saving,setSaving]=useState(false);
 function chooseLeader(id){const l=leaderData(leaders,id);setForm({...form,lider_id:id,lider_nombre:l.nombre||"",zona:l.zona||""})}
 async function save(e){e.preventDefault();if(+form.asistentes>+form.convocados)return alert("Los asistentes no pueden superar a los convocados.");setSaving(true);const {data,error}=await supabase.from("cafes_barrio").insert({lider_id:+form.lider_id,fecha:form.fecha,barrio:form.barrio,zona:form.zona,personas_convocadas:+form.convocados,personas_asistentes:+form.asistentes,descripcion:form.descripcion,estado:form.estado}).select().single();if(error){alert(error.message);setSaving(false);return}try{await uploadFiles(form.fotos,"CAFE_BARRIO",data.id,"FOTOGRAFIA");setForm(initial);await reload();alert("Actividad guardada en Supabase.")}catch(err){alert("La actividad se guardó, pero faltó una evidencia: "+err.message);await reload()}setSaving(false)}
 async function removeRecord(id){if(!confirm("¿Eliminar esta actividad?"))return;const {error}=await supabase.from("cafes_barrio").delete().eq("id",id);if(error)alert(error.message);else await reload()}
 async function migrateLocal(){const local=JSON.parse(localStorage.getItem("cafeRecords")||"[]");if(!local.length)return alert("No hay registros locales para migrar.");let ok=0;for(const r of local){const {error}=await supabase.from("cafes_barrio").insert({lider_id:+r.lider_id,fecha:r.fecha,barrio:r.barrio,zona:r.zona,personas_convocadas:+r.convocados||0,personas_asistentes:+r.asistentes||0,descripcion:r.descripcion||"Sin descripción",estado:"Realizada"});if(!error)ok++}if(ok===local.length)localStorage.removeItem("cafeRecords");await reload();alert(`${ok} de ${local.length} registros locales migrados.`)}
 return <main style={E.content}><div style={{...E.row,justifyContent:"space-between"}}><div><h1>Café a tu Barrio</h1><p style={{color:"#64748b"}}>Información centralizada en Supabase.</p></div><button type="button" style={{...E.btn,...E.white}} onClick={migrateLocal}>Migrar datos locales</button></div><form style={{...E.card,marginBottom:20}} onSubmit={save}><div style={E.row}><Field label="Líder responsable *"><select required style={E.input} value={form.lider_id} onChange={e=>chooseLeader(e.target.value)}><option value="">Seleccione un líder</option>{leaders.map(l=><option key={l.id} value={l.id}>{l.nombre}</option>)}</select></Field><Field label="Zona"><input readOnly style={{...E.input,background:"#f1f5f9"}} value={form.zona}/></Field></div><br/><div style={E.row}><Field label="Fecha *"><input required type="date" style={E.input} value={form.fecha} onChange={e=>setForm({...form,fecha:e.target.value})}/></Field><Field label="Barrio *"><input required style={E.input} value={form.barrio} onChange={e=>setForm({...form,barrio:e.target.value})}/></Field><Field label="Estado"><select style={E.input} value={form.estado} onChange={e=>setForm({...form,estado:e.target.value})}>{["Planeada","Programada","Realizada","Reprogramada","Cancelada"].map(x=><option key={x}>{x}</option>)}</select></Field></div><br/><div style={E.row}><Field label="Personas convocadas *"><input required min="0" type="number" style={E.input} value={form.convocados} onChange={e=>setForm({...form,convocados:e.target.value})}/></Field><Field label="Personas asistentes *"><input required min="0" type="number" style={E.input} value={form.asistentes} onChange={e=>setForm({...form,asistentes:e.target.value})}/></Field></div><br/><Field label="Descripción *"><textarea required rows="4" style={E.input} value={form.descripcion} onChange={e=>setForm({...form,descripcion:e.target.value})}/></Field><br/><Field label="Registro fotográfico"><input type="file" accept="image/*" multiple style={E.input} onChange={e=>setForm({...form,fotos:[...e.target.files]})}/></Field><br/><button disabled={saving} style={{...E.btn,...E.blue}}>{saving?"Guardando...":"Guardar actividad"}</button></form><h2>Actividades registradas</h2><div style={E.grid}>{records.length?records.map(r=><article key={r.id} style={E.card}><h3>{r.barrio}</h3><p><b>Líder:</b> {r.lider_nombre}</p><p><b>Zona:</b> {r.zona}</p><p><b>Fecha:</b> {fecha(r.fecha)} | <b>Estado:</b> {r.estado}</p><p><b>Convocados:</b> {r.convocados} | <b>Asistentes:</b> {r.asistentes}</p><p>{r.descripcion}</p><p><b>Fotografías:</b> {r.fotos?.length||0}</p><button style={{...E.btn,...E.red}} onClick={()=>removeRecord(r.id)}>Eliminar</button></article>):<p style={E.block}>No hay actividades registradas.</p>}</div></main>
}
function CoursesModule({leaders,records,reload,uploadFiles}){
 const initial={lider_id:"",lider_nombre:"",zona:"",nombre:"",fecha:"",lugar:"",instructor:"",participantes:"",asistentes:"",estado:"Realizada",lista:[],duplicadosCurso:0,evidencias:[],certificados:[]}; const [form,setForm]=useState(initial),[leaderFilter,setLeaderFilter]=useState("Todos"),[saving,setSaving]=useState(false);
 function chooseLeader(id){const l=leaderData(leaders,id);setForm({...form,lider_id:id,lider_nombre:l.nombre||"",zona:l.zona||""})}
 async function readCourseExcel(file){if(!file)return setForm({...form,lista:[],duplicadosCurso:0,asistentes:""});try{const bytes=await file.arrayBuffer(),wb=XLSX.read(bytes,{type:"array"}),rows=XLSX.utils.sheet_to_json(wb.Sheets[wb.SheetNames[0]],{defval:""});const parsed=rows.map(r=>({documento:norm(r.NumeroDocumento||r.numero_documento||r.Documento||r.documento).replace(/\.0$/, ""),nombre:norm(r.NombreCompleto||r.nombre_completo||r.Nombre||r.nombre),telefono:norm(r.Telefono||r.telefono),zona:form.zona})).filter(r=>/^\d{1,10}$/.test(r.documento)&&r.nombre);const seen=new Set(),unique=[],duplicates=[];parsed.forEach(r=>{if(seen.has(r.documento))duplicates.push(r);else{seen.add(r.documento);unique.push(r)}});setForm({...form,lista:unique,duplicadosCurso:duplicates.length,asistentes:String(unique.length)});alert(`Excel leído: ${unique.length} personas únicas y ${duplicates.length} repetidas.`)}catch(err){alert("No se pudo leer el Excel: "+err.message)}}
 async function save(e){e.preventDefault();if(+form.asistentes>+form.participantes)return alert("La asistencia no puede superar a los participantes.");if(!form.lista.length)return alert("Carga el Excel de asistentes.");setSaving(true);const {data,error}=await supabase.from("cursos").insert({lider_id:+form.lider_id,nombre:form.nombre,fecha:form.fecha,lugar:form.lugar,instructor:form.instructor,zona:form.zona,participantes_inscritos:+form.participantes,estado:form.estado}).select().single();if(error){alert(error.message);setSaving(false);return}try{for(const person of form.lista){const {data:p,error:pe}=await supabase.from("personas").upsert({numero_documento:person.documento,nombre_completo:person.nombre,telefono:person.telefono,zona:person.zona},{onConflict:"numero_documento"}).select("id").single();if(pe)throw pe;const {error:ae}=await supabase.from("asistentes_cursos").insert({curso_id:data.id,persona_id:p.id});if(ae)throw ae}await uploadFiles(form.evidencias,"CURSO",data.id,"EVIDENCIA");await uploadFiles(form.certificados,"CURSO",data.id,"CERTIFICADO");setForm(initial);await reload();alert("Curso y asistentes guardados en Supabase.")}catch(err){alert("El curso se creó, pero faltó procesar información: "+err.message);await reload()}setSaving(false)}
 async function removeRecord(id){if(!confirm("¿Eliminar este curso?"))return;const {error}=await supabase.from("cursos").delete().eq("id",id);if(error)alert(error.message);else await reload()}
 async function migrateLocal(){const local=JSON.parse(localStorage.getItem("courseRecords")||"[]");if(!local.length)return alert("No hay cursos locales para migrar.");let ok=0;for(const r of local){const {data,error}=await supabase.from("cursos").insert({lider_id:+r.lider_id,nombre:r.nombre,fecha:r.fecha,lugar:r.lugar,instructor:r.instructor,zona:r.zona,participantes_inscritos:+r.participantes||0,estado:"Realizada"}).select().single();if(error)continue;for(const person of r.lista||[]){const {data:p}=await supabase.from("personas").upsert({numero_documento:person.documento,nombre_completo:person.nombre,telefono:person.telefono,zona:r.zona},{onConflict:"numero_documento"}).select("id").single();if(p)await supabase.from("asistentes_cursos").insert({curso_id:data.id,persona_id:p.id})}ok++}if(ok===local.length)localStorage.removeItem("courseRecords");await reload();alert(`${ok} de ${local.length} cursos migrados.`)}
 function statsForLeader(id){const courses=records.filter(r=>String(r.lider_id)===String(id)),counts={};courses.forEach(c=>(c.lista||[]).forEach(person=>counts[person.documento]=(counts[person.documento]||0)+1));return{courses,total:courses.reduce((a,c)=>a+(c.lista?.length||+c.asistentes||0),0),unique:Object.keys(counts).length,repeated:Object.values(counts).filter(n=>n>1).length,counts}}
 function repeatedInCourse(course){const st=statsForLeader(course.lider_id);return (course.lista||[]).filter(p=>(st.counts[p.documento]||0)>1)}
 const summaryLeaders=leaders.map(l=>({leader:l,stats:statsForLeader(l.id)})).filter(x=>x.stats.courses.length),visible=leaderFilter==="Todos"?records:records.filter(r=>String(r.lider_id)===leaderFilter);
 return <main style={E.content}><div style={{...E.row,justifyContent:"space-between"}}><div><h1>Cursos</h1><p style={{color:"#64748b"}}>Cursos y asistentes centralizados en Supabase.</p></div><button type="button" style={{...E.btn,...E.white}} onClick={migrateLocal}>Migrar datos locales</button></div><section style={{...E.card,marginBottom:20}}><div style={{...E.row,justifyContent:"space-between"}}><h2>Resumen general por líder</h2><select style={{...E.input,width:260,marginTop:0}} value={leaderFilter} onChange={e=>setLeaderFilter(e.target.value)}><option value="Todos">Todos los líderes</option>{leaders.map(l=><option key={l.id} value={String(l.id)}>{l.nombre}</option>)}</select></div><Table headers={["Líder","Zona","Cursos","Participaciones","Beneficiarios únicos","Personas repetidas"]} rows={summaryLeaders.map(x=>[x.leader.nombre,x.leader.zona,x.stats.courses.length,x.stats.total,x.stats.unique,x.stats.repeated])}/></section><form style={{...E.card,marginBottom:20}} onSubmit={save}><h2>Registrar curso</h2><div style={E.row}><Field label="Líder responsable *"><select required style={E.input} value={form.lider_id} onChange={e=>chooseLeader(e.target.value)}><option value="">Seleccione un líder</option>{leaders.map(l=><option key={l.id} value={l.id}>{l.nombre}</option>)}</select></Field><Field label="Zona"><input readOnly style={{...E.input,background:"#f1f5f9"}} value={form.zona}/></Field></div><br/><Field label="Nombre del curso *"><input required style={E.input} value={form.nombre} onChange={e=>setForm({...form,nombre:e.target.value})}/></Field><br/><div style={E.row}><Field label="Fecha *"><input required type="date" style={E.input} value={form.fecha} onChange={e=>setForm({...form,fecha:e.target.value})}/></Field><Field label="Lugar *"><input required style={E.input} value={form.lugar} onChange={e=>setForm({...form,lugar:e.target.value})}/></Field><Field label="Instructor *"><input required style={E.input} value={form.instructor} onChange={e=>setForm({...form,instructor:e.target.value})}/></Field><Field label="Estado"><select style={E.input} value={form.estado} onChange={e=>setForm({...form,estado:e.target.value})}>{["Planeada","Programada","Realizada","Reprogramada","Cancelada"].map(x=><option key={x}>{x}</option>)}</select></Field></div><br/><div style={E.row}><Field label="Participantes inscritos *"><input required min="0" type="number" style={E.input} value={form.participantes} onChange={e=>setForm({...form,participantes:e.target.value})}/></Field><Field label="Asistentes únicos"><input readOnly type="number" style={{...E.input,background:"#f1f5f9"}} value={form.asistentes}/></Field></div><br/><div style={E.block}><b>Excel de asistentes *</b><p>Columnas: NumeroDocumento, NombreCompleto y Telefono.</p><input required type="file" accept=".xlsx,.xls,.csv" onChange={e=>readCourseExcel(e.target.files?.[0]||null)}/></div><br/><div style={E.row}><Field label="Evidencias"><input type="file" multiple style={E.input} onChange={e=>setForm({...form,evidencias:[...e.target.files]})}/></Field><Field label="Certificados"><input type="file" multiple accept=".pdf,image/*" style={E.input} onChange={e=>setForm({...form,certificados:[...e.target.files]})}/></Field></div><br/><button disabled={saving} style={{...E.btn,...E.blue}}>{saving?"Guardando...":"Guardar curso"}</button></form><h2>Cursos registrados</h2><div style={E.grid}>{visible.length?visible.map(r=>{const repeated=repeatedInCourse(r);return <article key={r.id} style={E.card}><h3>{r.nombre}</h3><p><b>Líder:</b> {r.lider_nombre}</p><p><b>Zona:</b> {r.zona}</p><p><b>Fecha:</b> {fecha(r.fecha)} | <b>Estado:</b> {r.estado}</p><p><b>Lugar:</b> {r.lugar} | <b>Instructor:</b> {r.instructor}</p><p><b>Inscritos:</b> {r.participantes} | <b>Beneficiarios únicos:</b> {r.lista?.length||0}</p><p><b>Repetidos:</b> {repeated.length} | <b>Evidencias:</b> {r.evidencias?.length||0} | <b>Certificados:</b> {r.certificados?.length||0}</p><button style={{...E.btn,...E.red}} onClick={()=>removeRecord(r.id)}>Eliminar</button></article>}):<p style={E.block}>No hay cursos registrados.</p>}</div></main>
}
function Table({headers,rows}){return <div style={{overflowX:"auto"}}><table style={E.table}><thead><tr style={{background:"#eff6ff"}}>{headers.map(h=><th key={h} style={E.cell}>{h}</th>)}</tr></thead><tbody>{rows.length?rows.map((r,i)=><tr key={i}>{r.map((v,j)=><td key={j} style={E.cell}>{v}</td>)}</tr>):<tr><td colSpan={headers.length} style={E.cell}>Sin datos</td></tr>}</tbody></table></div>}
